# minipack

a simple pack tool just for fun!

only implements `entry` and `output` like webpack.

## install

``` bash
npm link
```

## usage

in `package.json`

``` json
{
    "scripts": {
        "pack": "minipack"
    }
}
```

in shell

``` bash
npm run pack
```
